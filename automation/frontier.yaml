# Interim SOURCE-OF-TRUTH for the research-state dashboard sections (frontier, safety, claim
# boundaries). Hand-maintained WITH provenance until scan_artifacts.py (Stage 6) can derive these
# from the ledger + frozen-protocol twins + thesis_table_21. Update by hand when frontier facts
# change; every number here must be traceable to a committed artifact.
schema_version: 1
interim: true
supersede_at: "Stage 6 (scan_artifacts.py derives frontier/claim facts from ledger + frozen twins)"

primary_operating_point: "PGD eps=0.030, window-level, white-box, seed-42 LeNet family"

counters:
  validation_gate_reads_consumed: 0     # instrumented once val-pilot-gate lands
  frozen_test_reads_consumed: 1

milestones:
  - id: R90F10
    label: "R90F10 (startup / research goal)"
    definition: "recall > 0.90 AND FAR < 0.10 @ eps=0.030"
    status: not_met
    evidence_level: none
  - id: F20
    label: "F20 (thesis fallback)"
    definition: "recall >= 0.80 AND FAR <= 0.20 @ eps=0.030"
    status: post_hoc_only
    evidence_level: val_post_hoc
    note: "A threshold satisfying F20 exists on saved scores but is NOT a frozen read at eps=0.030."
  - id: H15_eps015
    label: "H15 @ eps=0.015"
    definition: "recall > 0.85 AND FAR < 0.15 @ eps=0.015"
    status: pass
    evidence_level: frozen_test
    detail: "TP 41/45, recall 0.9111 (Wilson 0.793-0.965), FAR 0.1319 (Wilson 0.104-0.166); clean recall 0.9778 / FAR 0.0901 at same tau"
    provenance: "results/defense_attempt_inventory/pgd_epsilon_frontier/frozen_test_read/FROZEN_H15_TEST_RESULT.md"

best_validation_frontier:
  operating_point: "PGD eps=0.030"
  reference_model: "G1 LeNet (best checkpoint)"
  val_pgd_auroc: 0.876
  val_recall_at_far_le_10: 0.477
  evidence_level: val_only
  note: "Six training-time interventions + BASAT (TRADES/GAIRAT/SAT) stall ~0.876; BiLSTM pivot worse (0.726). See REPRESENTATION_CEILING_CAPSTONE.md."
  advisor_read: "R90F10 @ eps=0.030 will not come from more attempts in the closed LeNet score-axis family (test oracle there ~recall 0.533 @ FAR<=0.15); needs a new method class or a scoped lower-eps claim."

frozen_protocol_registry:
  - protocol_id: "H15-TEST-EPS0015-AFAC-20260705"
    epsilon: 0.015
    model: "AFAC_maxscore (LeNet)"
    status: "READ-CONSUMED"
    verdict: "PASS"
    detail: "TP 41/45, recall 0.9111, FAR 0.1319; success rule TP>=39 AND FP<=68 satisfied"

test_split_guard:
  status: "warn-only"
  note: "PreToolUse hook active (.claude/settings.json), WARN-ONLY: warns on --split test/legacy and export/gate scripts invoked without --split, but does NOT block. Warning mechanism (commit dbf658a): one WARN-ONLY line to stderr + exit 1 — non-blocking under hook rules (only exit 2 blocks), transcript-auditable as a hook_non_blocking_error attachment. Safe commands exit 0 silently. export_probability_predictions.py:63 and adaptive_gate_attack.py:116 still default to --split test. Blocking mode is a future upgrade."

validation_read_log:
  status: "not_instrumented"
  note: "0 logged; created with val-pilot-gate."

ledger_audit:
  status: "hand_maintained"
  ledger: "results/defense_attempt_inventory/defense_attempt_results_long.csv"
  note: "ledger-sync --audit not yet built; no automated re-derivation check."

claim_boundaries:
  source: "results/thesis_table_21_claim_boundary_evidence_matrix.csv"
  thesis_safe_now:
    - "software-level FGSM/PGD adversarial stress test"
    - "defended-vs-undefended descriptive comparison"
    - "H15@eps=0.015 frozen PASS (with mandatory disclosures)"
  research_goal_only:
    - "R90F10 @ eps=0.030"
    - "any certified / clinical / deployment / OTA claim"
    - "cross-dataset generalization"

overleaf_ready:
  note: "SUPERSEDED at Stage 6 by automation/artifact_manifest.csv (scan_artifacts.py). The dashboard §8-9 now renders from the manifest; this block is the fallback only when the manifest is absent."
  eligible_now:
    - "H15@eps=0.015 frozen PASS (with disclosures: eps chosen by validation-only search; Gate-5 argmax caveat clean-acc 0.694; window-level/digital/white-box/seed-42)"
