# Curated Reference Evidence — SOURCE-OF-TRUTH (Research OS layer, D2d-1).
# Hand-curated, user-approved pointers to ALREADY-COMMITTED evidence artifacts. This file records
# frozen numbers already published in committed results/ artifacts — it does NOT generate, derive,
# or infer any metric, and it must NEVER override the manifest's evidence_level. A future Result
# Explorer resolution step (D2d-2, separate approval) must cross-check every entry's provenance
# paths against automation/artifact_manifest.csv and refuse (provenance_mismatch) if the cited
# manifest evidence_level disagrees with this file's evidence_level claim.
schema_version: 1

reference_results:
  - key: H15_eps0015_frozen
    goal: fall_detection
    dataset: sensefi_ut_har
    attack: pgd
    epsilon: 0.015
    split: test
    evidence_level: frozen-test
    threshold: 0.168754
    metrics:
      TP: 41
      FN: 4
      FP: 60
      TN: 395
      Rfall: 0.911111
      FAR: 0.131868
    protocol_id: "H15-TEST-EPS0015-AFAC-20260705"
    provenance:
      confusion_csv: "results/defense_attempt_inventory/pgd_epsilon_frontier/frozen_test_read/frozen_h15_test_confusion.csv"
      result_report: "results/defense_attempt_inventory/pgd_epsilon_frontier/frozen_test_read/FROZEN_H15_TEST_RESULT.md"
      required_manifest_evidence_level: frozen-test
      frontier_registry_ref: "automation/frontier.yaml frozen_protocol_registry"
    clean_disclosure_companion:
      condition: clean_disclosure
      epsilon: 0.0
      threshold: 0.168754
      TP: 44
      FN: 1
      FP: 41
      TN: 414
      Rfall: 0.977778
      FAR: 0.09011
    disclosures:
      - "eps=0.015, NOT eps=0.030 — this is not the R90F10 operating point"
      - "not R90F10 — FAR (0.131868) remains above the 0.10 R90F10 ceiling"
      - "frozen-test evidence — a one-shot protocol read, not clinical or deployment evidence"
      - "clean-condition disclosure recorded at the same threshold (see clean_disclosure_companion)"

  - key: AFAC_eps0030_F20_posthoc
    goal: fall_detection
    dataset: sensefi_ut_har
    attack: pgd
    epsilon: 0.030
    split: test
    evidence_level: test-post-hoc
    method: optionB_AFAC
    threshold: 0.153246
    threshold_rule: "post-hoc fall-score threshold sweep via fall_probability"
    metrics:
      TP: 36
      FN: 9
      FP: 91
      TN: 364
      Rfall: 0.800000
      FAR: 0.200000
    provenance:
      selector: "F20_reached=True AND split=test AND attack=pgd AND epsilon=0.03"
      selector_note: "unique row in the ledger's test/pgd/eps=0.03 pool (1 of 158) — the canonical F20 operating point, not a hand choice"
      source_file: "results/safety_guided_defense/variantH_dual_tail_budget/adaptive_lagrangian_far_constrained/optionB/seed42/test_eval/optionB_maxscore_pgd_probabilities_test_epsilon_0_03.csv"
      required_manifest_evidence_level: test-post-hoc
    disclosures:
      - "post-hoc only — a satisfying threshold exists on saved scores, not a frozen read"
      - "not a frozen read — no frozen protocol was executed at eps=0.030 for this method"
      - "F20 knife-edge caveat — the D8b analysis found F20-qualifying thresholds can be as narrow as ~0.00028 wide; treat as fragile, not robust"
      - "not R90F10 — FAR (0.200000) is well above the 0.10 R90F10 ceiling"
